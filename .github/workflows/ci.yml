name: CI Build

on:
  pull_request:
    branches:
      - main
      - next
  push:
    branches:
      - main
      - next

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      release-mode: false
      retention-days: 7
